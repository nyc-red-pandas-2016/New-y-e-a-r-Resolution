<h1> New Year Resolutions </h1>

<ul><% @questions.each do |q| %>
  <li>Votes: <%=q.views %> Answers: <%=q.answers.length %> Votes: <%=q.votes.reduce(:+) %> Asked:<a href="/questions/<%= q.id %>"><%= q.title %> </a><br>
    <% q.tags.each do |tag| %>
      <%= tag.name %>
    <% end %>
  </li>
<% end %></ul>
